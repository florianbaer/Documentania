<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mvvm="http://prismlibrary.com/"
    xmlns:views="clr-namespace:Document.Wpf.Views"
    xmlns:extensions="clr-namespace:Document.Wpf.Extensions"
    x:Class="Document.Wpf.Views.AllDocumentsView"
             mc:Ignorable="d" 
             mvvm:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="5*"/>
        </Grid.ColumnDefinitions>

        <views:DocumentsListView Grid.Column="0" Documents="{Binding DocumentViewModels}"  Selected="{Binding Selected}"/>

        <views:DocumentDetailView Grid.Column="1" DocumentSource="{Binding Selected}"/>
        
        <GridSplitter Grid.Column="2" ResizeBehavior="PreviousAndNext" VerticalAlignment="Stretch" Width="4" />
        
        <WebBrowser Grid.Column="3" extensions:WebBrowserExtension.BindableSource="{Binding Path=Selected.Path}"/>
    </Grid>
</UserControl>















