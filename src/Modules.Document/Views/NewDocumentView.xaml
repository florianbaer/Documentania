<UserControl x:Class="Modules.Document.Views.NewDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Modules.Document.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="7*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Column="0" Grid.Row="0" VerticalAlignment="Center">Name:</Label>
        <TextBox Grid.Column="1" Grid.Row="0" Text="{Binding Name}" HorizontalAlignment="Stretch" VerticalAlignment="Center"></TextBox>
        
        <Label Grid.Column="0" Grid.Row="1" VerticalAlignment="Center">Date received:</Label>
        <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding DateReceived}" HorizontalAlignment="Stretch" VerticalAlignment="Center"></TextBox>
        
        <Label Grid.Column="0" Grid.Row="2" VerticalAlignment="Center">Path:</Label>
        <Grid  Grid.Column="1" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" IsReadOnly="True" VerticalAlignment="Center" Text="{Binding Path}"/>
        <Button x:Name="LoadDocumentButton" VerticalAlignment="Center" Grid.Column="1" HorizontalAlignment="Right">Load Document</Button>
        </Grid>
        <ItemsControl Grid.Column="0" Grid.Row="3" Grid.RowSpan="2" Grid.ColumnSpan="2">
            <Label >Tags:</Label>
        </ItemsControl>
    </Grid>
</UserControl>
